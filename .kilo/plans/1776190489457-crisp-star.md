# Implement template.html Layout into Kasir

## Goal
Implement template.html's **layout/structure** into `resources/views/kasir/create.blade.php` while keeping **SB Admin color palette** (`#4e73df`, `#1cc88a`, etc.)

## What's Already Done (from previous session)
- ✅ Cart empty state with large shopping-cart icon
- ✅ Cart items with flexbox layout + icon
- ✅ Qty controls styling  
- ✅ Payment toggle with 2px border
- ✅ Summary with dashed border
- ✅ Cart item icon (box icon)

## Changes Needed

### 1. Products: Change from Table to Card Grid
**File**: `resources/views/kasir/create.blade.php` lines 790-860

**Before**: Bootstrap table (`<table class="table table-hover product-table">`)
**After**: CSS Grid cards (like template lines 156-219)

Changes:
- Replace table HTML with grid of product cards
- Card shows: icon, name, price, stock badge
- Click card to add qty=1
- Add qty input inside card for custom quantity
- Use SB Admin colors: primary `#4e73df`, success `#1cc88a` for price

### 2. Product Card Styling
**CSS to add**:
```css
.products-grid {
    display: grid;
    grid-template-columns: repeat(auto-fill, minmax(160px, 1fr));
    gap: 1rem;
    padding: 1rem;
}

.product-card {
    background: #fff;
    border: 2px solid #e3e6f0;
    border-radius: 0.5rem;
    padding: 1rem;
    cursor: pointer;
    transition: all 0.2s;
    display: flex;
    flex-direction: column;
    align-items: center;
    text-align: center;
    gap: 0.5rem;
}

.product-card:hover {
    border-color: #4e73df;
    transform: translateY(-2px);
    box-shadow: 0 4px 12px rgba(78,115,223,.2);
}

.product-img {
    width: 60px;
    height: 60px;
    background: #f8f9fc;
    border-radius: 0.5rem;
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 1.5rem;
    color: #4e73df;
}

.product-name {
    font-weight: 700;
    font-size: 0.8rem;
    color: #3a3b45;
}

.product-price {
    font-weight: 800;
    font-size: 0.9rem;
    color: #4e73df;
}
```

### 3. Search Bar Update
**File**: lines 777-787

Update to match template style (lines 93-124):
- Remove Bootstrap input-group wrapper
- Use floating search icon with input
- 2px border, border-radius

### 4. Cart Panel Refinement
Already partially done - ensure alignment with template:
- Item subtotal color should be `#3a3b45` (not primary)
- Remove button needs hover state matching template (red background)

### 5. JavaScript Updates (Minimal)

**Keep ALL existing IDs and function names** - only update selectors for new HTML structure:

1. **attachAddItemListeners()** - Update to work with product cards instead of table rows
2. **search functionality** - May need to update result rendering

**Preserve**:
- All DOM IDs unchanged
- All function names unchanged  
- All event handlers use delegation

## Implementation Order

1. Add new CSS for products-grid and product-card
2. Create new product grid HTML (replace table)
3. Update search HTML style
4. Update JS attachAddItemListeners for cards
5. Verify all existing JS works

## SB Admin Colors Reference
```
--primary: #4e73df
--success: #1cc88a  
--danger: #e74c3c
--warning: #f6c23e
--gray-100: #f8f9fc
--gray-600: #858796
--text: #3a3b45
```